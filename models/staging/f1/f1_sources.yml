version: 2

sources:
  - name: f1_raw
    meta:
      external_location: "data/{name}.csv"
    # This config block now applies to EVERY table listed below
    config:
      options:
        nullstr: '\N'
    tables:
      - name: drivers
      - name: results
      - name: races
      - name: constructors
      - name: status